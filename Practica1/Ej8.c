#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    int edad;
    scanf("%d",&edad);
    if (edad >= 40)
        printf("Tiene %d o mas",edad);
    else
        printf("Tiene menos de 40,tiene %d ",edad);
    */

    /*
    int total, x = 1;
    while (x <= 10) {
        total += x;
        ++x;
    }
    printf("Total = %d",total);
    */

    /*
    int valor;
    scanf("%d",&valor);
    if ((valor % 2)==0)
        printf("El valor es par");
    else
        printf("El valor es impar");

    */

    /*
    int x, y;
    scanf("%d",&x);
    scanf("%d",&y);
    printf("x + y = %d ",x+y);
    */

    return 0;
}
