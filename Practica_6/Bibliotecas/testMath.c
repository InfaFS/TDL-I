#include <stdio.h>
#include "imath.h"

int main(){
    int x=5;
    int y=3;
    printf("%d\n",potencia(x,y));
    printf("%d\n",cuadrado(x));
    printf("El absoluto del numero %d es %d",x,absoluto(x));
    return 0;
}